import { useState } from 'react';
import { createTrip } from '../../features/trips/tripService';
export function PublishPage(){const [title,setTitle]=useState('灵山岛外海铁板航次');const [price,setPrice]=useState(500);async function submit(){await createTrip({boatId:'b1',captainId:'c1',portId:'p1',title,date:'2026-07-08',startTime:'06:00',endTime:'14:00',seaArea:'灵山岛外海',price,totalSeats:8,targetFish:['蓝点马鲛','真鲷'],note:'船东发布测试航次'});alert('已发布：如果 Supabase 配置正确，会写入数据库；未配置时仅演示。')}return <section className="section"><h1 className="section-title">发布航次</h1><p className="muted">船东快速发布发船计划</p><div className="card" style={{padding:20,display:'grid',gap:16,marginTop:22}}><label>航次标题<input className="input" value={title} onChange={e=>setTitle(e.target.value)} /></label><label>价格/人<input className="input" type="number" value={price} onChange={e=>setPrice(Number(e.target.value))}/></label><label>出发时间<input className="input" value="2026-07-08 06:00 - 14:00" readOnly/></label><label>目标鱼种<input className="input" value="蓝点马鲛 / 真鲷" readOnly/></label><button className="btn btn-primary" onClick={submit}>发布航次</button></div></section>}
